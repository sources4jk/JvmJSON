plugins {
    id 'maven-publish'
    id 'org.jetbrains.kotlin.jvm' version '1.9.22'
}

version = '0.9.6'
group = "s4jk.jvm.json"

repositories {
    mavenCentral()
    maven { url 'https://jitpack.io' }
}

kotlin {
    jvmToolchain(11)
}